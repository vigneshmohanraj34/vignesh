import java.util.*;

class removeduplicate {
	public static void main (String[] args) {
		Scanner s=new Scanner(System.in);
		LinkedHashSet <String> s1=new LinkedHashSet<String>(Arrays.asList(s.nextLine().split("")));
		System.out.print(s1);
	}
}
